# Only used on debian. Defaults for puppetmaster - sourced by /etc/init.d/master

# Enable puppetmaster service? 
START=no

# Startup options
DAEMON_OPTS=""

# On what port should the puppet master listen? (default: 8140)
PORT=8140
